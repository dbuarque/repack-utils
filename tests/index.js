import { RepackUtils } from '../src/index';

const should = require('should');

describe('RepackBase', function() {
    it('should be and object', function () {
        var utils = new RepackUtils();
    });
});
